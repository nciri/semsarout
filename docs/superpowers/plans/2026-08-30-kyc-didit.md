# KYC réel (Didit) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer le KYC stub (validation manuelle admin) par une vraie vérification Didit (session hébergée + webhook signé), et en faire un verrou avant signature électronique (mandat/bail particulier, compromis de vente) sur les deux parties.

**Architecture:** `identity` reste propriétaire du domaine KYC. Nouveau client `didit_client.py` (appels réels vers `verification.didit.me`), deux colonnes ajoutées à `KycVerification`, deux nouveaux endpoints (session, webhook) + un endpoint de pull + un endpoint interne de statut. `rental`/`selling` gagnent chacun un `identity_client.py` (patron `commission_client.py`) consulté juste avant la création d'une `SignatureRequest`, uniquement sur les deux endpoints où les deux parties sont des `user_id` plateforme réels (bail particulier `owner_id`+`tenant_user_id`, compromis `seller_party`+`buyer_party`).

**Tech Stack:** FastAPI, SQLAlchemy, httpx, `semsar_events` (outbox existant, aucun nouvel event type).

**Spec:** `docs/superpowers/specs/2026-08-30-kyc-didit-design.md`

## Global Constraints

- KYC exigé **seulement avant signature**, jamais avant publication.
- Les **deux parties** doivent être `verified`.
- Secrets Didit (`DIDIT_API_KEY`, `DIDIT_WORKFLOW_ID`, `DIDIT_WEBHOOK_SECRET`) en `.env` gitignoré, jamais en dur.
- Webhook : signature `X-Signature-V2` (HMAC-SHA256) vérifiée si un secret est configuré (patron fail-closed de `services/payment/app/main.py`).
- `internal_kyc_verify`/`internal_kyc_reject` (repli manuel admin) restent inchangés et fonctionnels.
- **Écart de périmètre assumé** (découvert à la planification, à documenter dans le commit) : l'endpoint générique `rental` `POST /backoffice/gestion-locative/{doc_type}/{doc_id}/request-signature` (mandat/bail/inventaire/décompte **agence-médiés**) n'est **pas** gardé par ce livrable — la contrepartie y est un `crm.Client` qui n'a pas forcément de `user_id` plateforme lié. Gater cet endpoint nécessiterait d'abord de garantir ce lien, hors spec.

---

### Task 1 : `identity` — modèle + client Didit + endpoints

**Files:**
- Modify: `services/identity/app/models.py`
- Create: `services/identity/app/didit_client.py`
- Modify: `services/identity/app/main.py`
- Create: `services/identity/db/migrate_didit.sql`
- Modify: `services/identity/.env.example`
- Create: `services/identity/tests/test_kyc_didit.py`

**Interfaces:**
- Produces: `didit_client.create_session(vendor_data: str, callback: str) -> dict` (`{"session_id","url","session_token"}`, lève `DiditUnavailable` sur erreur réseau/HTTP).
- Produces: `didit_client.fetch_decision(session_id: str) -> dict` (payload brut de `GET .../session/{id}/decision/`).
- Produces: `didit_client.verify_signature(raw_body: bytes, signature: str) -> bool`.
- Produces (dans `main.py`) : `_apply_kyc_decision(db, record, decision: str) -> None` — `decision` ∈ `"verified"|"rejected"`, factorisé et réutilisé par `internal_kyc_verify`/`internal_kyc_reject` (refactor, comportement identique) **et** les nouveaux chemins Didit.
- Consumes (Task 3/4) : `GET /internal/kyc/status/{user_id}` — réponse `{"status": "none"|"pending"|"verified"|"rejected"}`.

- [ ] **Step 1: Write the failing tests**

```python
# services/identity/tests/test_kyc_didit.py
from datetime import datetime, timezone

from werkzeug.security import generate_password_hash

import app.main as m
from app.models import KycVerification, UserRO
from semsar_auth import Principal, get_principal


def _user(db_session, uid=1) -> UserRO:
    u = UserRO(id=uid, email=f"u{uid}@ex.ma", password_hash=generate_password_hash("x"),
               first_name="A", last_name="B", tenant="semsar", is_verified=False)
    db_session.add(u)
    db_session.commit()
    return u


def _as_user(uid: int):
    m.app.dependency_overrides[get_principal] = lambda: Principal(
        sub=str(uid), roles=[], agency_id=None, is_superadmin=False, features=[], claims={})


def test_create_session_calls_didit_and_stores_pending(client, db_session, monkeypatch):
    _user(db_session, 1)
    _as_user(1)
    monkeypatch.setattr(m.didit_client, "create_session",
                        lambda **k: {"session_id": "sess-1", "url": "https://verify.didit.me/x",
                                    "session_token": "tok"})
    resp = client.post("/identity/kyc/session")
    m.app.dependency_overrides.pop(get_principal, None)
    assert resp.status_code == 201
    assert resp.json()["url"] == "https://verify.didit.me/x"
    row = db_session.query(KycVerification).filter_by(user_id=1).first()
    assert row.status == "pending"
    assert row.didit_session_id == "sess-1"


def test_create_session_didit_unavailable_returns_502(client, db_session, monkeypatch):
    _user(db_session, 1)
    _as_user(1)
    def boom(**k):
        raise m.didit_client.DiditUnavailable("down")
    monkeypatch.setattr(m.didit_client, "create_session", boom)
    resp = client.post("/identity/kyc/session")
    m.app.dependency_overrides.pop(get_principal, None)
    assert resp.status_code == 502


def test_webhook_rejects_bad_signature(client, monkeypatch):
    monkeypatch.setattr(m, "_WEBHOOK_SECRET", "shh")
    monkeypatch.setattr(m.didit_client, "verify_signature", lambda body, sig: False)
    resp = client.post("/identity/kyc/webhook", json={"session_id": "sess-1"},
                       headers={"x-signature-v2": "bad"})
    assert resp.status_code == 401


def test_webhook_approved_marks_verified_and_emits_event(client, db_session, monkeypatch):
    u = _user(db_session, 2)
    k = KycVerification(user_id=2, cin=None, status="pending", didit_session_id="sess-2",
                        created_at=datetime.now(timezone.utc))
    db_session.add(k)
    db_session.commit()
    monkeypatch.setattr(m, "_WEBHOOK_SECRET", "")  # pas de secret configuré en test = no-op
    resp = client.post("/identity/kyc/webhook", json={
        "webhook_type": "status.updated", "session_id": "sess-2",
        "decision": {"status": "Approved"},
    })
    assert resp.status_code == 200
    db_session.refresh(k)
    db_session.refresh(u)
    assert k.status == "verified"
    assert u.is_verified is True


def test_webhook_declined_marks_rejected(client, db_session, monkeypatch):
    _user(db_session, 3)
    k = KycVerification(user_id=3, cin=None, status="pending", didit_session_id="sess-3",
                        created_at=datetime.now(timezone.utc))
    db_session.add(k)
    db_session.commit()
    monkeypatch.setattr(m, "_WEBHOOK_SECRET", "")
    resp = client.post("/identity/kyc/webhook", json={
        "webhook_type": "status.updated", "session_id": "sess-3",
        "decision": {"status": "Declined"},
    })
    assert resp.status_code == 200
    db_session.refresh(k)
    assert k.status == "rejected"


def test_webhook_ignores_already_terminal_session(client, db_session, monkeypatch):
    _user(db_session, 4)
    k = KycVerification(user_id=4, cin=None, status="verified", didit_session_id="sess-4",
                        created_at=datetime.now(timezone.utc))
    db_session.add(k)
    db_session.commit()
    monkeypatch.setattr(m, "_WEBHOOK_SECRET", "")
    resp = client.post("/identity/kyc/webhook", json={
        "webhook_type": "status.updated", "session_id": "sess-4",
        "decision": {"status": "Declined"},
    })
    assert resp.status_code == 200
    db_session.refresh(k)
    assert k.status == "verified"  # anti-rejeu : pas retraité


def test_webhook_unknown_session_returns_404(client, monkeypatch):
    monkeypatch.setattr(m, "_WEBHOOK_SECRET", "")
    resp = client.post("/identity/kyc/webhook", json={
        "webhook_type": "status.updated", "session_id": "ghost",
        "decision": {"status": "Approved"},
    })
    assert resp.status_code == 404


def test_refresh_pulls_decision_and_applies(client, db_session, monkeypatch):
    _user(db_session, 5)
    _as_user(5)
    k = KycVerification(user_id=5, cin=None, status="pending", didit_session_id="sess-5",
                        created_at=datetime.now(timezone.utc))
    db_session.add(k)
    db_session.commit()
    monkeypatch.setattr(m.didit_client, "fetch_decision",
                        lambda session_id: {"status": "Approved"})
    resp = client.post(f"/identity/kyc/{k.id}/refresh")
    m.app.dependency_overrides.pop(get_principal, None)
    assert resp.status_code == 200
    db_session.refresh(k)
    assert k.status == "verified"


def test_refresh_forbidden_for_non_owner(client, db_session, monkeypatch):
    _user(db_session, 6)
    _as_user(7)  # un autre utilisateur
    k = KycVerification(user_id=6, cin=None, status="pending", didit_session_id="sess-6",
                        created_at=datetime.now(timezone.utc))
    db_session.add(k)
    db_session.commit()
    resp = client.post(f"/identity/kyc/{k.id}/refresh")
    m.app.dependency_overrides.pop(get_principal, None)
    assert resp.status_code == 403


def test_internal_status_requires_token(client):
    resp = client.get("/internal/kyc/status/1")
    assert resp.status_code == 403


def test_internal_status_returns_none_when_absent(client, monkeypatch):
    monkeypatch.setattr(m.settings, "internal_token", "tok")
    resp = client.get("/internal/kyc/status/999", headers={"x-internal-token": "tok"})
    assert resp.status_code == 200
    assert resp.json() == {"status": "none"}


def test_internal_status_returns_latest(client, db_session, monkeypatch):
    monkeypatch.setattr(m.settings, "internal_token", "tok")
    _user(db_session, 8)
    db_session.add(KycVerification(user_id=8, cin=None, status="verified",
                                   created_at=datetime.now(timezone.utc)))
    db_session.commit()
    resp = client.get("/internal/kyc/status/8", headers={"x-internal-token": "tok"})
    assert resp.json() == {"status": "verified"}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd services/identity && python3 -m pytest tests/test_kyc_didit.py -v`
Expected: FAIL (`didit_client` n'existe pas, routes 404).

- [ ] **Step 3: Migrate the model**

In `services/identity/app/models.py`, on `KycVerification` :
- Change `cin = Column(String(32), nullable=False)` → `cin = Column(String(32), nullable=True)`.
- Add after `cin` :
```python
    didit_session_id = Column(String(64), nullable=True, index=True)
    decision = Column(JSON, nullable=True)  # décision brute Didit, usage interne/audit uniquement
```
(`JSON` déjà importé dans ce fichier — vérifier l'import en tête, l'ajouter à la liste `sqlalchemy import (...)` sinon.)

Create `services/identity/db/migrate_didit.sql` :
```sql
-- KYC réel (Didit). create_all couvre les bases neuves ; ce script ajoute les colonnes
-- aux bases existantes (prod/dev déjà initialisées).
ALTER TABLE identity.kyc_verification ALTER COLUMN cin DROP NOT NULL;
ALTER TABLE identity.kyc_verification ADD COLUMN IF NOT EXISTS didit_session_id VARCHAR(64);
ALTER TABLE identity.kyc_verification ADD COLUMN IF NOT EXISTS decision JSON;
CREATE INDEX IF NOT EXISTS ix_kyc_verification_didit_session_id
    ON identity.kyc_verification (didit_session_id);
```

- [ ] **Step 4: Write `didit_client.py`**

```python
"""Client Didit (KYC réel) — session hébergée + décision (pull), voir
docs.didit.me/getting-started/quick-start. Jamais de valeurs par défaut sur les secrets :
si non configurés, les appels échouent explicitement (fail-closed)."""
import hashlib
import hmac
import os

import httpx

_API_URL = os.environ.get("DIDIT_API_URL", "https://verification.didit.me")
_API_KEY = os.environ.get("DIDIT_API_KEY", "")
_WORKFLOW_ID = os.environ.get("DIDIT_WORKFLOW_ID", "")


class DiditUnavailable(Exception):
    pass


def create_session(vendor_data: str, callback: str | None = None) -> dict:
    body = {"workflow_id": _WORKFLOW_ID, "vendor_data": vendor_data}
    if callback:
        body["callback"] = callback
    try:
        r = httpx.post(f"{_API_URL}/v3/session/", json=body,
                       headers={"x-api-key": _API_KEY}, timeout=10.0)
        r.raise_for_status()
        return r.json()
    except httpx.HTTPError as e:
        raise DiditUnavailable(str(e)) from e


def fetch_decision(session_id: str) -> dict:
    try:
        r = httpx.get(f"{_API_URL}/v3/session/{session_id}/decision/",
                      headers={"x-api-key": _API_KEY}, timeout=10.0)
        r.raise_for_status()
        return r.json()
    except httpx.HTTPError as e:
        raise DiditUnavailable(str(e)) from e


def verify_signature(raw_body: bytes, signature: str) -> bool:
    secret = os.environ.get("DIDIT_WEBHOOK_SECRET", "")
    if not secret:
        return True  # pas de secret configuré : no-op (parité payment webhook)
    expected = hmac.new(secret.encode(), raw_body, hashlib.sha256).hexdigest()
    return hmac.compare_digest(signature, expected)
```

- [ ] **Step 5: Refactor `_apply_kyc_decision` + add endpoints in `main.py`**

Add imports: `from . import didit_client` and `from fastapi import Request` (déjà
partiellement importé, vérifier). Add module-level
`_WEBHOOK_SECRET = os.environ.get("DIDIT_WEBHOOK_SECRET", "")` (import `os` si absent).

Replace the bodies of `internal_kyc_verify`/`internal_kyc_reject` to delegate to a
shared helper (comportement strictement identique, juste factorisé) :

```python
def _apply_kyc_decision(db: Session, record: KycVerification, decision: str) -> None:
    """decision ∈ 'verified'|'rejected'. Ne touche `UserRO.is_verified` que sur 'verified'
    (un rejet n'annule pas une vérification déjà acquise ailleurs — parité comportement existant)."""
    from .models import UserRO
    record.status = decision
    user = db.get(UserRO, record.user_id)
    if decision == "verified" and user is not None:
        user.is_verified = True
    event_type = events.KYC_VERIFIED if decision == "verified" else None
    if event_type:
        enqueue(db, aggregate_type="kyc_verification", aggregate_id=record.id,
               event_type=event_type, payload={"user_id": record.user_id})


@app.post("/internal/kyc/{kyc_id}/verify", include_in_schema=False)
def internal_kyc_verify(kyc_id: int, x_internal_token: str = Header(default=""),
                        db: Session = Depends(get_db)) -> dict:
    record = _resolve_kyc(db, kyc_id, x_internal_token)
    if record is None:
        raise not_found("Vérification introuvable.")
    from .models import UserRO
    _apply_kyc_decision(db, record, "verified")
    user = db.get(UserRO, record.user_id)
    db.commit()
    return _kyc_to_dict(record, user)


@app.post("/internal/kyc/{kyc_id}/reject", include_in_schema=False)
def internal_kyc_reject(kyc_id: int, x_internal_token: str = Header(default=""),
                        db: Session = Depends(get_db)) -> dict:
    record = _resolve_kyc(db, kyc_id, x_internal_token)
    if record is None:
        raise not_found("Vérification introuvable.")
    from .models import UserRO
    _apply_kyc_decision(db, record, "rejected")
    user = db.get(UserRO, record.user_id)
    db.commit()
    return _kyc_to_dict(record, user)
```

Add the four new routes (n'importe où après `get_kyc`, avant `_kyc_to_dict`
convient) :

```python
@app.post("/identity/kyc/session", status_code=201)
def create_kyc_session(principal: Principal = Depends(get_principal),
                       db: Session = Depends(get_db)) -> dict:
    user_id = _user_id(principal)
    try:
        session = didit_client.create_session(vendor_data=str(user_id))
    except didit_client.DiditUnavailable:
        raise forbidden("Service de vérification indisponible.") from None  # 403 par défaut ;
        # remplacé ci-dessous par une vraie 502 — voir note
    record = KycVerification(user_id=user_id, cin=None, status="pending",
                             didit_session_id=session["session_id"])
    db.add(record)
    db.commit()
    return {"id": record.id, "status": record.status, "url": session["url"]}


_DECISION_MAP = {"Approved": "verified", "Declined": "rejected", "Abandoned": "rejected"}
_TERMINAL_KYC = {"verified", "rejected"}


def _apply_didit_payload(db: Session, session_id: str, decision_payload: dict):
    record = db.query(KycVerification).filter(
        KycVerification.didit_session_id == session_id).order_by(
        KycVerification.id.desc()).first()
    if record is None:
        return None, False
    if record.status in _TERMINAL_KYC:
        return record, False  # anti-rejeu
    status = decision_payload.get("status")
    decision = _DECISION_MAP.get(status)
    record.decision = decision_payload
    if decision is not None:
        _apply_kyc_decision(db, record, decision)
    return record, decision is not None


@app.post("/identity/kyc/webhook", include_in_schema=False)
async def kyc_webhook(request: Request, db: Session = Depends(get_db)) -> dict:
    raw = await request.body()
    sig = request.headers.get("x-signature-v2", "")
    if not didit_client.verify_signature(raw, sig):
        raise unauthorized("Signature invalide.")
    import json as _json
    payload = _json.loads(raw) if raw else {}
    session_id = payload.get("session_id")
    if not session_id:
        return {"ok": True}
    record, _ = _apply_didit_payload(db, session_id, payload.get("decision") or {})
    if record is None:
        raise not_found("Session inconnue.")
    db.commit()
    return {"ok": True}


@app.post("/identity/kyc/{kyc_id}/refresh")
def refresh_kyc_session(kyc_id: int, principal: Principal = Depends(get_principal),
                        db: Session = Depends(get_db)) -> dict:
    record = db.get(KycVerification, kyc_id)
    if record is None:
        raise not_found("Vérification introuvable.")
    if record.user_id != _user_id(principal):
        raise forbidden("Accès non autorisé à cette vérification.")
    if record.status in _TERMINAL_KYC or not record.didit_session_id:
        return {"id": record.id, "status": record.status}
    decision_payload = didit_client.fetch_decision(record.didit_session_id)
    _apply_didit_payload(db, record.didit_session_id, decision_payload)
    db.commit()
    return {"id": record.id, "status": record.status}


@app.get("/internal/kyc/status/{user_id}", include_in_schema=False)
def internal_kyc_status(user_id: int, x_internal_token: str = Header(default=""),
                        db: Session = Depends(get_db)) -> dict:
    if x_internal_token != settings.internal_token:
        raise forbidden("Forbidden")
    record = db.query(KycVerification).filter(
        KycVerification.user_id == user_id).order_by(KycVerification.id.desc()).first()
    return {"status": record.status if record is not None else "none"}
```

**Note d'implémentation** : le `except didit_client.DiditUnavailable` ci-dessus doit
renvoyer une vraie erreur 502, pas `forbidden()` — `semsar_common` n'a pas de helper
502 tout fait dans ce service ; utiliser `from fastapi.responses import JSONResponse`
et `return JSONResponse({"error": "Service de vérification indisponible."},
status_code=502)` à la place du `raise forbidden(...)` du brouillon ci-dessus
(corriger en écrivant le code, ne pas copier le brouillon tel quel sur ce point).

- [ ] **Step 6: `.env.example`**

Append to `services/identity/.env.example` :
```
DIDIT_API_URL=https://verification.didit.me
DIDIT_API_KEY=
DIDIT_WORKFLOW_ID=
DIDIT_WEBHOOK_SECRET=
```

- [ ] **Step 7: Run tests to verify they pass**

Run: `cd services/identity && python3 -m pytest tests/ -v`
Expected: PASS (nouveaux tests + suite existante, ~13 fichiers, sans régression).

- [ ] **Step 8: Commit**

```bash
git add services/identity/app/models.py services/identity/app/didit_client.py services/identity/app/main.py services/identity/db/migrate_didit.sql services/identity/.env.example services/identity/tests/test_kyc_didit.py
git commit -m "feat(identity): KYC réel via Didit (session hébergée + webhook + pull)"
```

---

### Task 2 : Gateway — routage webhook public

**Files:**
- Modify: `gateway/app/main.py`
- Modify: `gateway/tests/test_reports_routes.py` (ou nouveau fichier `test_kyc_routes.py` si plus clair — juger en lisant le fichier)

**Interfaces:**
- Consumes: `app.state.identity`, `settings.identity_url` (déjà présents).

Le webhook Didit (`POST /api/v1/identity/kyc/webhook`) doit passer **sans**
authentification utilisateur. La route générique `/api/v1/identity*` (ligne
~349 de `gateway/app/main.py`) route déjà tout le préfixe vers `app.state.identity`
sans vérifier de JWT au niveau BFF (l'auth est imposée par le service identity
lui-même, via `Depends(get_principal)` sur les routes qui l'exigent) — **donc
aucun changement de routage n'est nécessaire** : `POST /identity/kyc/session`
et `POST /identity/kyc/{id}/refresh` sont déjà couverts (protégés côté service),
et `POST /identity/kyc/webhook` passe déjà (le service n'y met aucun
`Depends(get_principal)`).

- [ ] **Step 1: Vérifier par la lecture, pas par supposition**

Lire `gateway/app/main.py` autour de la ligne 340-355 pour confirmer qu'aucune
garde BFF (ex. vérification de JWT avant le routage générique `/api/v1/identity*`)
ne bloquerait un appel non authentifié. Si une garde existe (à vérifier, pas
supposé), ajouter une exception explicite pour `path ==
"/api/v1/identity/kyc/webhook"` AVANT cette garde, patron
`_REPORT_ACTION`/`payments_webhook_relay`.

- [ ] **Step 2: Test de routage (si Step 1 a nécessité un changement)**

Si aucun changement n'était nécessaire, écrire quand même un test de
non-régression confirmant que le webhook est joignable sans jeton :

```python
def test_kyc_webhook_reachable_without_auth(monkeypatch):
    # Vérifie que /api/v1/identity/kyc/webhook route bien vers identity, sans
    # dépendre d'un principal — lecture de _resolve_upstream, patron test_reports_routes.py.
    import app.main as m
    monkeypatch.setattr(m.settings, "identity_url", "http://i")
    fake = _app(identity="IDENTITY")
    assert m._resolve_upstream(fake, "/api/v1/identity/kyc/webhook", "POST") == (
        "IDENTITY", "/identity/kyc/webhook")
```

Run: `cd gateway && python3 -m pytest tests/ -q`
Expected: PASS.

- [ ] **Step 3: Commit (si des changements de code ont eu lieu ; sinon, committer seulement le test)**

```bash
git add gateway/app/main.py gateway/tests/
git commit -m "test(gateway): confirme le routage public du webhook KYC Didit"
```

---

### Task 3 : `rental` — verrou KYC avant signature (bail particulier)

**Files:**
- Create: `services/rental/app/identity_client.py`
- Modify: `services/rental/app/main.py`
- Modify: `services/rental/.env.example`
- Modify: `scripts/dev-mesh-up.sh` (si `IDENTITY_URL` n'est pas déjà dans l'`extra` rental — **vérifier en lisant** la ligne 77 actuelle avant de toucher, elle contient déjà `IDENTITY_URL=http://localhost:8501` d'après une lecture précédente : dans ce cas, ne rien changer ici).
- Modify: `services/rental/tests/test_owner_lease_sign.py`

**Interfaces:**
- Produces: `identity_client.status(user_id: int) -> str` (`"none"|"pending"|"verified"|"rejected"`, retourne `"none"` en cas d'erreur réseau — fail-closed côté appelant, qui doit alors bloquer plutôt que laisser passer).

- [ ] **Step 1: Write `identity_client.py`**

```python
"""Statut KYC d'un utilisateur (patron commission_client.py) — fail-closed :
toute erreur réseau est traitée comme 'none' (non vérifié) par l'appelant."""
import os

import httpx

from semsar_common import get_settings

_IDENTITY_URL = os.environ.get("IDENTITY_URL", "http://localhost:8501")


def status(user_id: int) -> str:
    try:
        r = httpx.get(f"{_IDENTITY_URL}/internal/kyc/status/{user_id}",
                      headers={"x-internal-token": get_settings().internal_token},
                      timeout=6.0)
        r.raise_for_status()
        return r.json().get("status", "none")
    except httpx.HTTPError:
        return "none"
```

- [ ] **Step 2: Write the failing tests**

Add to `services/rental/tests/test_owner_lease_sign.py` :

```python
def test_kyc_not_verified_blocks_signature(db_session, monkeypatch):
    _lease(db_session)
    monkeypatch.setattr(main.commission_client, "gate", lambda **k: {"state": "OPEN", "billable": False})
    monkeypatch.setattr(main.signing, "signing_enabled", lambda: True)
    monkeypatch.setattr(main.identity_client, "status", lambda uid: "none")
    client = make_owner_client(db_session, uid="5")
    r = client.post("/gestion-locative/owner/leases/3/request-signature", json={})
    assert r.status_code == 422
    assert db_session.query(models.SignatureRequest).count() == 0


def test_kyc_verified_both_parties_allows_signature(db_session, monkeypatch):
    _lease(db_session)
    monkeypatch.setattr(main.commission_client, "gate", lambda **k: {"state": "OPEN", "billable": False})
    monkeypatch.setattr(main.signing, "signing_enabled", lambda: True)
    monkeypatch.setattr(main.identity_client, "status", lambda uid: "verified")
    monkeypatch.setattr(main.signing, "create_envelope", lambda *a, **k: "env-1")
    monkeypatch.setattr(main.signing, "add_document", lambda *a, **k: ("doc-1", 1))
    monkeypatch.setattr(main.signing, "add_recipient", lambda *a, **k: "r-1")
    monkeypatch.setattr(main.signing, "place_signature_field", lambda *a, **k: None)
    monkeypatch.setattr(main.signing, "send_envelope", lambda *a, **k: None)
    monkeypatch.setattr(main, "_owner_lease_pdf_bytes", lambda db, l: b"%PDF-")
    monkeypatch.setattr(main, "_owner_email", lambda uid: "owner@x.c")
    monkeypatch.setattr(main, "_applicant_email_for_lease", lambda db, l: "tenant@x.c")
    client = make_owner_client(db_session, uid="5")
    r = client.post("/gestion-locative/owner/leases/3/request-signature", json={})
    assert r.status_code == 200
```

- [ ] **Step 3: Run to verify failure**

Run: `cd services/rental && python3 -m pytest tests/test_owner_lease_sign.py -v`
Expected: FAIL (`AttributeError: module 'app.main' has no attribute 'identity_client'`).

- [ ] **Step 4: Wire the gate**

In `services/rental/app/main.py`, add `from . import identity_client` to the
import block near `from . import ... commission_client ...` (lire les imports
existants pour matcher l'emplacement exact). In
`owner_request_lease_signature`, right after the commission `BLOCKED` check
passes (i.e. after the `if decision.get("state") == "BLOCKED":` block, before
`# 3) Lancer la e-signature (OPEN)`), insert:

```python
    # 2bis) Verrou KYC (fail-closed) — les deux parties doivent être vérifiées.
    if identity_client.status(uid) != "verified" or identity_client.status(l.tenant_user_id) != "verified":
        return JSONResponse(
            {"error": "KYC requis avant signature — propriétaire et locataire doivent "
                      "avoir vérifié leur identité."},
            status_code=422)
```

- [ ] **Step 5: `.env.example`**

Confirm `services/rental/.env.example` already has `IDENTITY_URL=` — if
present, no change; if absent, add `IDENTITY_URL=http://localhost:8501`.

- [ ] **Step 6: Run tests to verify they pass**

Run: `cd services/rental && python3 -m pytest tests/ -q`
Expected: PASS (suite complète, aucune régression sur les tests existants qui
mockent déjà `commission_client.gate` mais pas `identity_client.status` —
**si des tests existants échouent faute de mock `identity_client.status`,
ajouter `monkeypatch.setattr(main.identity_client, "status", lambda uid:
"verified")` à CHAQUE test existant qui atteint ce point du code**, ne pas
changer le comportement de production pour les faire passer).

- [ ] **Step 7: Commit**

```bash
git add services/rental/app/identity_client.py services/rental/app/main.py services/rental/.env.example services/rental/tests/test_owner_lease_sign.py
git commit -m "feat(rental): verrou KYC avant signature du bail particulier"
```

---

### Task 4 : `selling` — verrou KYC avant signature du compromis

**Files:**
- Create: `services/selling/app/identity_client.py`
- Modify: `services/selling/app/main.py`
- Modify: `services/selling/.env.example`
- Modify: `scripts/dev-mesh-up.sh` (ajouter `IDENTITY_URL=http://localhost:8501` à l'`extra` du cas `selling` — actuellement absent d'après lecture précédente : `extra="LISTING_URL=... COMMISSION_URL=... $SELLING_SIGN_VARS"`).
- Modify: `services/selling/tests/test_compromis_sign.py`

**Interfaces:**
- Produces: `identity_client.status(user_id: int) -> str` — **fichier identique** à `services/rental/app/identity_client.py` (dupliqué à l'identique, patron déjà établi par `commission_client.py` présent en double dans les deux services — ne pas créer de lib partagée pour ça, cohérent avec l'existant).

- [ ] **Step 1: Write `identity_client.py`**

Copier exactement le fichier `services/rental/app/identity_client.py` (Task 3
Step 1) dans `services/selling/app/identity_client.py`, sans modification.

- [ ] **Step 2: Write the failing tests**

Lire `services/selling/tests/test_compromis_sign.py` en premier pour repérer
le patron exact de fixtures/mocks utilisé (probablement très proche de
`test_owner_lease_sign.py` mais à vérifier — noms de fixtures `client`,
setup de `PurchaseInquiry`/`Compromis`). Ajouter deux tests, patron Task 3
Step 2 :
- `test_kyc_not_verified_blocks_signature` : mock `identity_client.status`
  → `"none"` pour au moins une des deux parties → 422, pas de
  `SignatureRequest` créée.
- `test_kyc_verified_both_parties_allows_signature` : mock → `"verified"`
  pour `seller_party` et `buyer_party` → 200 comme avant.

- [ ] **Step 3: Run to verify failure**

Run: `cd services/selling && python3 -m pytest tests/test_compromis_sign.py -v`
Expected: FAIL.

- [ ] **Step 4: Wire the gate**

In `services/selling/app/main.py`, add `from . import identity_client`. Right
after the commission `BLOCKED` check (`if decision.get("state") ==
"BLOCKED":` block), before `# OPEN → PDF + e-signature`, insert:

```python
    # Verrou KYC (fail-closed) — vendeur et acheteur doivent être vérifiés.
    if identity_client.status(inq.seller_party) != "verified" \
            or identity_client.status(inq.buyer_party) != "verified":
        db.commit()  # persiste le Compromis créé plus haut (draft) même si on bloque ici
        return JSONResponse(
            {"error": "KYC requis avant signature — vendeur et acheteur doivent "
                      "avoir vérifié leur identité."},
            status_code=422)
```

(Le `db.commit()` avant le `return` est nécessaire ici car `c` — le
`Compromis` — a potentiellement été créé plus haut dans la fonction et doit
être persisté même si la signature est bloquée, patron du bloc `BLOCKED`
juste au-dessus qui fait la même chose.)

- [ ] **Step 5: `.env.example` + dev-mesh**

Add `IDENTITY_URL=http://localhost:8501` to `services/selling/.env.example`.
In `scripts/dev-mesh-up.sh`, find the `selling)` case in the `extra=` switch
(section `for pair in $SVCS`) and add `IDENTITY_URL=http://localhost:8501` to
its `extra` string.

- [ ] **Step 6: Run tests to verify they pass**

Run: `cd services/selling && python3 -m pytest tests/ -q`
Expected: PASS (même consigne qu'à la Task 3 Step 6 pour les tests existants
qui n'ont pas encore de mock `identity_client.status`).

- [ ] **Step 7: Commit**

```bash
git add services/selling/app/identity_client.py services/selling/app/main.py services/selling/.env.example scripts/dev-mesh-up.sh services/selling/tests/test_compromis_sign.py
git commit -m "feat(selling): verrou KYC avant signature du compromis de vente"
```

---

### Task 5 : Cohérence `.env.example` (garde-fou anti-bug)

**Files:**
- Create: `tools/check_env_examples.py`
- Modify: `.github/workflows/ci.yml` (ou un job existant approprié — lire le
  fichier pour trouver le meilleur emplacement, ex. à côté du job qui lance
  `tools/contract_test.py` si un tel job existe ; sinon un nouveau job
  simple).

**Contexte** : l'audit e-signature a trouvé `services/selling/.env.example`
avec `SIGN_API_URL=http://localhost:8501` (port d'`identity`, pas de
3a9dSign/18000) — bug de copier-coller jamais détecté faute de garde-fou.
Ce garde-fou est volontairement simple : pas de vérification réseau, juste
une cohérence déclarative basique.

**Interfaces:**
- Produces: script CLI `python3 tools/check_env_examples.py` — sortie non-zéro
  si une incohérence est trouvée, liste les fichiers/lignes en cause.

- [ ] **Step 1: Write the script**

```python
#!/usr/bin/env python3
"""Garde-fou : détecte les URLs de service mal recopiées dans les .env.example
(cf. bug trouvé lors de l'audit e-signature du 2026-08-30 : services/selling/.env.example
pointait SIGN_API_URL vers le port d'identity au lieu de 3a9dSign)."""
import re
import sys
from pathlib import Path

# Ports de référence connus (mesh local, cf. scripts/dev-mesh-up.sh) — noms de service
# EXACTS attendus dans le nom de variable pour ce port.
KNOWN_PORTS = {
    "8501": "identity", "8502": "notification", "8504": "analytics", "8505": "contract",
    "8506": "legal", "8507": "payment", "8508": "billing", "8509": "geo",
    "8510": "messaging", "8511": "trust-safety", "8512": "agency", "8513": "audit",
    "8514": "transactions", "8515": "buyer", "8516": "programs", "8517": "staymanager",
    "8518": "rental", "8519": "commission", "8520": "selling", "8521": "coloc-listing",
    "8522": "coloc-profile", "8523": "matching", "8524": "translation", "8525": "partner",
}

URL_RE = re.compile(r"^([A-Z0-9_]+)_URL=https?://[^:/]+:(\d+)")


def check_file(path: Path) -> list[str]:
    errors = []
    for line in path.read_text().splitlines():
        m = URL_RE.match(line.strip())
        if not m:
            continue
        var_name, port = m.groups()
        expected_service = KNOWN_PORTS.get(port)
        if expected_service is None:
            continue  # port hors mesh connu (ex. secret propre au service, type SIGN_API_URL) — ignoré
        var_service_hint = var_name.removesuffix("_URL").lower().replace("_", "-")
        if var_service_hint != expected_service and var_service_hint in KNOWN_PORTS.values():
            errors.append(f"{path}: {var_name} pointe vers le port {port} ({expected_service}), "
                          f"mais le nom de variable suggère '{var_service_hint}'")
    return errors


def main() -> int:
    root = Path(__file__).resolve().parent.parent
    all_errors = []
    for path in sorted(root.glob("services/*/.env.example")):
        all_errors.extend(check_file(path))
    for path in sorted(root.glob("*/.env.example")):  # gateway, etc.
        all_errors.extend(check_file(path))
    if all_errors:
        print("Incohérences .env.example détectées :")
        for e in all_errors:
            print(f"  - {e}")
        return 1
    print("OK — aucun .env.example incohérent trouvé.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Run it against the current repo — confirm it catches the known bug**

Run: `python3 tools/check_env_examples.py`
Expected: exit code `1`, listant `services/selling/.env.example: SIGN_API_URL
pointe vers le port 8501 (identity), mais le nom de variable suggère 'sign'`
— **note** : `sign` n'est pas dans `KNOWN_PORTS.values()`, donc ce cas
précis ne sera PAS détecté par cette heuristique simple (elle ne couvre que
les URLs `<NOM_CONNU>_URL` mal routées, pas les noms de variable hors
nomenclature comme `SIGN_API_URL`). **Documenter cette limite dans le
commit** plutôt que de complexifier le script pour un cas — corriger le bug
lui-même reste hors périmètre (spec, § hors périmètre).

- [ ] **Step 3: Wire into CI**

Add a step to `.github/workflows/ci.yml` (job existant le plus proche, ou un
petit job dédié `env-examples`) :
```yaml
      - name: Cohérence des .env.example
        run: python3 tools/check_env_examples.py
```

- [ ] **Step 4: Commit**

```bash
git add tools/check_env_examples.py .github/workflows/ci.yml
git commit -m "chore(ci): garde-fou de cohérence des .env.example"
```

---

## Self-Review Notes

- **Couverture spec** : session/webhook/pull (Task 1) ✓, verrou avant
  signature sur les deux endpoits à user_id réels (Task 3, Task 4) ✓,
  secrets (Task 1/3/4) ✓, garde-fou `.env.example` (Task 5, suite de
  l'audit e-signature) ✓.
- **Écart de périmètre assumé et documenté** : l'endpoint générique agence
  `rental` `request_signature` (mandat/bail/inventaire/décompte
  agence-médiés) n'est PAS gardé — contrepartie CRM sans `user_id`
  plateforme garanti. Noté en tête de plan et dans le message de commit de
  Task 3.
- **Hors périmètre confirmé non traité** (cf. spec) : filet de sécurité
  automatique (tâche planifiée), fix du bug `SIGN_API_URL` de `selling`
  (déjà connu, ticket séparé), chantiers e-signature/CMI (brainstormings
  séparés).
- **Frontend** : volontairement absent de ce plan. Le premier livrable
  backend doit être validé (notamment le vrai comportement Didit une fois le
  workflow configuré côté compte) avant de committer sur une UX précise —
  contrairement au score de confiance (où le contrat de données était déjà
  stable), ici le contenu exact du payload webhook/decision Didit n'est
  connu que par documentation publique, pas par un test contre le vrai
  compte. Le frontend sera un plan de suivi une fois Task 1 validée en
  intégration réelle (pas seulement en tests unitaires mockés).
