Round user image with optional green verified check and inline name/subtitle.

```jsx
<Avatar name="Sarah E." verified size={44} />
<Avatar name="Yassine E." showLabel subtitle="Étudiant" />
```
Falls back to initials when `src` is absent.
