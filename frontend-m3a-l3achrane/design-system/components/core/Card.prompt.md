White elevated surface — the base container for everything. Pass `hover` for interactive cards.

```jsx
<Card padding={24} hover>…</Card>
```
16px radius, hairline border, soft shadow. No colored left-border accents.
