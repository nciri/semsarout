Filter / attribute pill. Static (amenity) or selectable (filter).

```jsx
<Chip icon="wifi">Wi-Fi</Chip>
<Chip selected onClick={toggle}>Meublé</Chip>
```
Selected state fills navy. Use `AmenityChip` in listing/ for the icon+label amenity variant.
