Thin-line glyph wrapper over Lucide (CDN); use for every icon in the system.

```jsx
<Icon name="shield-check" size={20} color="var(--verified)" />
```
Requires `<script src="https://unpkg.com/lucide@latest"></script>` on the page. Any Lucide name works.
