Icon-only control for toolbars, cards, headers (bell, video call, filters).

```jsx
<IconButton icon="bell" label="Notifications" variant="soft" round />
```
Variants: ghost · soft · outline · navy. Sizes sm/md/lg. Pass `round` for a circle.
