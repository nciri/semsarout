Underlined segmented nav (the Colocations / Résidences pattern).

```jsx
<Tabs tabs={[{label:"Colocations",value:"c",icon:"users"},{label:"Résidences",value:"r",icon:"building-2"}]} />
```
Controlled (`value`+`onChange`) or uncontrolled.
