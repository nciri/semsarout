[project]
name = "semsar-directory"
version = "0.1.0"
description = "SemsarOut — service directory (artisans partagés/privés + bons de travaux)."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "prometheus-fastapi-instrumentator>=7.0",
    "SQLAlchemy>=2.0",
    "psycopg[binary]>=3.1",
    "semsar-common",
    "semsar-auth",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["app"]
